# The sharded question bank (public/bank/{lang}/{category}.json) is generated by /trivia-gen and
# bundled into dist/client, served from ASSETS. This placeholder keeps the directory in git until then.
