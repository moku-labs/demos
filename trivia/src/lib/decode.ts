/**
 * @file Pure helper — decode a salted `answerCheck` to its correct slot (0–3). Anti-spoiler, not security.
 */

/**
 * Decode a salted answerCheck into the correct option slot index.
 *
 * @param _answerCheck - The obfuscated answer field from the bank.
 * @throws {Error} Always — skeleton stub, implemented in the build wave.
 * @example
 * ```ts
 * decode(answerCheck); // 1
 * ```
 */
export function decode(_answerCheck: string): number {
  throw new Error("not implemented");
}
