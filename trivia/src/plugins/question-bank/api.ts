/**
 * @file question-bank plugin — public API factory skeleton (load / next / grade / availability).
 */
import type { Api } from "./types";

/**
 * Build the question-bank API (host-side load/select/grade + availability).
 *
 * @throws {Error} Always — skeleton stub, implemented in the build wave.
 * @example
 * ```ts
 * createPlugin("questionBank", { api: createQuestionBankApi });
 * ```
 */
export function createQuestionBankApi(): Api {
  throw new Error("not implemented");
}
