/**
 * @file question-bank plugin — onInit lifecycle skeleton (registers slices + the seen-history intent).
 */

/**
 * Register the `bank` + `categories` slices and the `seen-history` intent (deps resolve in onInit).
 *
 * @throws {Error} Always — skeleton stub, implemented in the build wave.
 * @example
 * ```ts
 * createPlugin("questionBank", { onInit: initQuestionBank });
 * ```
 */
export function initQuestionBank(): void {
  throw new Error("not implemented");
}
