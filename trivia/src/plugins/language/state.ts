/**
 * @file language plugin — host-internal state factory skeleton.
 */
import type { State } from "./types";

/**
 * Build the initial (empty) per-peer vote map.
 *
 * @throws {Error} Always — skeleton stub, implemented in the build wave.
 * @example
 * ```ts
 * createPlugin("language", { createState: createLanguageState });
 * ```
 */
export function createLanguageState(): State {
  throw new Error("not implemented");
}
