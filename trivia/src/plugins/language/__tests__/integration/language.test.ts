import { describe, it } from "vitest";

describe("language (integration)", () => {
  it.todo("controller language-vote updates the languageVote slice on the replica");
  it.todo("after voteWindowMs the host confirms + the onConfirm callback fires once");
});
