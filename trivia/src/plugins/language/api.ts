/**
 * @file language plugin — public API factory skeleton (openVote / cancelVote / result).
 */
import type { Api } from "./types";

/**
 * Build the language-vote API (open the vote + arm the window, cancel, read the confirmed result).
 *
 * @throws {Error} Always — skeleton stub, implemented in the build wave.
 * @example
 * ```ts
 * createPlugin("language", { api: createLanguageApi });
 * ```
 */
export function createLanguageApi(): Api {
  throw new Error("not implemented");
}
