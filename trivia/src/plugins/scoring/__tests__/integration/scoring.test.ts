import { describe, it } from "vitest";

describe("scoring (integration)", () => {
  it.todo("host award over several rounds; the scores slice reaches the controller replica");
  it.todo("reset re-publishes a zeroed board on the replica");
});
