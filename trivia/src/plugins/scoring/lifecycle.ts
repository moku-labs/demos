/**
 * @file scoring plugin — onInit lifecycle skeleton (registers the `scores` slice + seeds an empty board).
 */

/**
 * Register the `scores` slice and seed an empty board (deps resolve in onInit).
 *
 * @throws {Error} Always — skeleton stub, implemented in the build wave.
 * @example
 * ```ts
 * createPlugin("scoring", { onInit: initScoring });
 * ```
 */
export function initScoring(): void {
  throw new Error("not implemented");
}
