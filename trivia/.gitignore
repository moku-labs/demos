# dependencies
node_modules

# build output
out
dist
*.tgz

# code coverage
coverage
*.lcov

# logs
logs
*.log
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local
.dev.vars

# Cloudflare / Wrangler
.wrangler
# wrangler.jsonc is a generated artifact — written by `dev`/`deploy` from src/server.ts (ids are
# account-captured at deploy time, not source). Never hand-write or commit it (atlas convention).
wrangler.jsonc

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Finder (macOS) folder config
.DS_Store

# Claude Code — local config is ignored, but the project SHIPS the /trivia-gen skill + command
# (a build deliverable; the skill extends the committed spec/TRIVIA_SKILL.md). Negate only those.
.claude/*
!.claude/skills
.claude/skills/*
!.claude/skills/trivia-gen
!.claude/commands
.claude/commands/*
!.claude/commands/trivia-gen.md

# Playwright MCP browser-session artifacts (console logs / page snapshots)
.playwright-mcp

# Planning artifacts
.planning

# /trivia-gen scratch (raw + reviewed shards before the deterministic encode into public/bank)
scratchpad
