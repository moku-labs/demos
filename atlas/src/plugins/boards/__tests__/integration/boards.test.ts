import { describe, it } from "vitest";

describe("boards (integration)", () => {
  it.todo("createApp wires realtime+attachments+kv+d1+boards; create → getBoardWithColumns");
  it.todo("delete updates the KV index and purges R2");
});
