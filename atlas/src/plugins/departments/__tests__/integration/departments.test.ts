import { describe, it } from "vitest";

describe("departments (integration)", () => {
  it.todo("createApp wires attachments+d1+departments; create → list reflects it");
  it.todo("deleting a department with boards purges its R2 blobs (no orphans)");
});
