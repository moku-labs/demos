import { describe, it } from "vitest";

describe("issues (integration)", () => {
  it.todo("createApp wires realtime+attachments+d1+issues; create → listForBoard → getDetail");
  it.todo("full property round-trip (status/priority/labels/assignees/due/estimate)");
});
