import { describe, it } from "vitest";

describe("realtime", () => {
  it.todo("broadcast resolves the board DO stub with the configured boardDo");
  it.todo("broadcast POSTs the exact serialized patch to /broadcast");
  it.todo("a stub .fetch rejection propagates to the caller");
});
