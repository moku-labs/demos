/**
 * @file Route map — the single source of truth for every addressable place in the app (web Rule R2:
 * ONE route table for build, SPA navigation, and link building).
 *
 * Skeleton: empty. Routes (boards · board · issue · department · activity deep-links) and the typed
 * `urls` builder derived from this map are added during the implementation waves.
 */
import { defineRoutes } from "@moku-labs/web/browser";

/** The application route map — one entry per addressable place (empty in the skeleton). */
export const routes = defineRoutes({});
